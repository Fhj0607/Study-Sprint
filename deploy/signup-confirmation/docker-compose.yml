services:
  signup-confirmation:
    image: nginx:alpine
    container_name: study-sprint-signup-confirmation
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
